# Merge freeze probe — do not merge

Non-runtime probe used only to prove that the required `merge-freeze-gate` status check blocks merging while the owner-controlled freeze issue is open.

This file must not ship as a product change.
