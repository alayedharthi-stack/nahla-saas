"""Commerce Conversation Reliability Gate (C1-PR1).

Executable harness that (a) runs the PR #1084 regression modules unchanged,
(b) exercises real runtime entry points with scripted models and fake
providers, (c) records proven baseline defects under a reviewed manifest, and
(d) labels future runtime contracts as unimplemented. See
``docs/engineering/commerce-reliability-gate.md``.
"""
