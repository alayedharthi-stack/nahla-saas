[phases.setup]
nixPkgs = ["nginx"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "sed \"s|\\${PORT}|${PORT:-3000}|g\" /app/nginx.conf > /tmp/nginx_resolved.conf && nginx -c /tmp/nginx_resolved.conf -g 'daemon off;'"
